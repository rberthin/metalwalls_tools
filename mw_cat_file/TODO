** files to add :
    - density
    - charge_density

** restart to add :
    - temperature.out     
    - trajectories.out    
    - polarization.out    
    - charges.out         
    - dipoles.out         
    - pressure.out        
    - box_parameters.out  
    - density 
    - charge_density

** Func restart.py + test if it doesn't match

** Size of files and set a limit
subprocess.run(['ls', '-l'], capture_output=True, text=True).stdout
